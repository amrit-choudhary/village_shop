#include "utils.h"

using namespace ME;

uint32_t ME::HashString2uint32(const char* str) {
    uint32_t hash = 5381;
    int c;
    while ((c = *str++)) {
        hash = ((hash << 5) + hash) + c;
    }
    return hash;
}
